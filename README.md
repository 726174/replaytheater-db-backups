# Replay Theater DB Backups
Database backups for [replaytheater.app](https://replaytheater.app)

The database currently uses Postgres version 18.1

Dumps are created using the `pg_dump` custom file format and can be restored using `pg_restore`
